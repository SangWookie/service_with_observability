spring:
  datasource:
    url: jdbc:mysql://server.legatalee.me:3306/digital_receipt
    username: receipt_manager
    password: digital123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc:
          batch_size: 50
server:
  port: 8000
